"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _path = _interopRequireDefault(require("path"));

var _fsExtra = _interopRequireDefault(require("fs-extra"));

var _readLocalManifest = _interopRequireDefault(require("./readLocalManifest"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var addPluginToManifest = (sanityDir, pluginName) => (0, _readLocalManifest.default)(sanityDir, 'sanity.json').then(manifest => {
  manifest.plugins = manifest.plugins || [];

  if (manifest.plugins.indexOf(pluginName) === -1) {
    manifest.plugins.push(pluginName);
  }

  return manifest;
}).then(manifest => _fsExtra.default.writeJson(_path.default.join(sanityDir, 'sanity.json'), manifest, {
  spaces: 2
}));

var _default = addPluginToManifest;
exports.default = _default;