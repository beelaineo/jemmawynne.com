"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getProviders;

var _client = _interopRequireDefault(require("part:@sanity/base/client"));

var _defaultLogin = _interopRequireDefault(require("config:@sanity/default-login"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getProviders() {
  var config = _defaultLogin.default || {};
  return _client.default.request({
    uri: '/auth/providers',
    withCredentials: true
  }).then(res => {
    var providers = res.providers,
        thirdPartyLogin = res.thirdPartyLogin;
    var customProviders = (config.providers && config.providers.entries ? config.providers.entries : []).map(provider => {
      provider.custom = true;
      provider.supported = thirdPartyLogin;
      return provider;
    });

    if (customProviders.length && config.providers && config.providers.mode === 'replace') {
      return Promise.resolve(customProviders);
    }

    return providers.concat(customProviders);
  });
}