"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = OnDropPlugin;

var _slateReact = require("slate-react");

// This plugin does for now ensure that nothing can be dragged onto void nodes and cause errors.
// Slate doesn't check this by itself.
function OnDropPlugin() {
  return {
    onDrop(event, editor, next) {
      var target = event.target;
      var node = (0, _slateReact.findNode)(target, editor);

      if (editor.query('isVoid', node)) {
        return editor;
      }

      return next();
    }

  };
}