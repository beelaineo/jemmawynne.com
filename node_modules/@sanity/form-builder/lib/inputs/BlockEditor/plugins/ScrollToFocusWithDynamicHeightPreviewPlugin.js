"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = ScrollToFocusWithDynamicHeightPreviewPlugin;

// This plugin tries to scroll the focusblock into view for 2 seconds after the editor is mounted.
// This makes it active long enough to bypass any quirks regarding scollheight as the
// dynamic height preview are loading.
function ScrollToFocusWithDynamicHeightPreviewPlugin(scrollContainer, scrollIntoView) {
  var isActive = true;
  setTimeout(() => {
    isActive = false;
  }, 1000);
  return {
    onSelect(selection, editor, next) {
      if (isActive && editor.value.focusBlock) {
        scrollIntoView(editor.value.focusBlock, {
          behavior: 'instant'
        });
      }

      return next();
    }

  };
}