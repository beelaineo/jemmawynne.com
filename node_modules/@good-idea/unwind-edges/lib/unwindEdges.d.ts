import { Paginated } from './types';
/**
 * unwindEdges
 * @export
 * @template EdgeType
 * @param {Paginated<EdgeType>} { edges, pageInfo }
 * @returns {UnwoundEdges<EdgeType>}
 */
export declare const unwindEdges: <EdgeType = any>({ edges, pageInfo }: Paginated<EdgeType>) => [import("./types").NodeWithCursor<EdgeType>[], import("./types").PaginationInfo];
