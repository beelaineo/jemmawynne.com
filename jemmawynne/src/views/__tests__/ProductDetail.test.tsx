import * as React from 'react'
import { ProductDetail } from '../ProductDetail'

describe('Product Detail (integration)', () => {
	it.skip('should add a product to checkout when the buy button is clicked', () => {
		/* */
	})

	it.skip('should display an updated description when the user selects a different variant', () => {
		/* */
	})

	it.skip('should display a "checkout now" message after adding an item', () => {
		/* */
	})
})
