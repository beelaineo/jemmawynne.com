schema: http://localhost:8080/.netlify/functions/graphql
generates:
  src/types/generated.ts:
    plugins:
      - typescript
    config:
      nonOptionalTypename: true
      scalars:
        DateTime: Date
        JSON: '{ [key: string]: any }'
      declarationKind:
        type: 'interface'
