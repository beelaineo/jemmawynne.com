schema: https://caazz4uw.api.sanity.io/v1/graphql/production/default
generates:
  types/generated.ts:
    plugins:
      - typescript
    config:
      nonOptionalTypename: true
      scalars:
        DateTime: Date
        JSON: '{ [key: string]: any }'
      declarationKind:
        type: 'interface'
